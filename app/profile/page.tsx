import UserProfilePage from '@/components/profile/UserProfilePage';

export const metadata = {
  title: 'User Profile & Candidate Dashboard | Panel AI',
  description: 'Track your AI interview practice stats, daily contribution heatmap, performance badges, and skill scores on Panel AI.',
};

export default function ProfilePage() {
  return <UserProfilePage />;
}
